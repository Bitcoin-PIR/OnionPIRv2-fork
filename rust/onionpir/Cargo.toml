[package]
name = "onionpir"
version = "0.2.0"
edition = "2021"
description = "Rust bindings for OnionPIRv2 (SEAL-free, BV key-switching)"
license = "MIT"

[lib]
name = "onionpir"
crate-type = ["rlib"]

[build-dependencies]
cmake = "0.1"
